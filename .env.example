# Database Configuration
# Neon Postgres connection string
# Format: postgresql://[user]:[password]@[host]/[database]?sslmode=require
DATABASE_URL=postgresql://neondb_owner:npg_1oedAQt8RjXn@ep-lingering-queen-ah27r5t5-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require

# Redis Configuration (Optional - for caching)
# Upstash Redis REST URL and Token
UPSTASH_REDIS_REST_URL=https://selected-elk-34280.upstash.io
UPSTASH_REDIS_REST_TOKEN=AYXoAAIncDJkMzhjYzFhNGYxMTI0ZDA1OTVlZTkzZWVlMDFmZTBhNnAyMzQyODA

# JNTUH Configuration
# Base URL for JNTUH results portal
JNTUH_RESULTS_BASE_URL=http://results.jntuh.ac.in/results/
# Default exam code (update when new results are released)
JNTUH_EXAM_CODE=1323
# Default result type (e.g., "grade", "cbcs")
JNTUH_RESULT_TYPE=grade

# Scraper Configuration
# Number of parallel workers for scraping (increased for R22 entire university)
SCRAPER_WORKER_COUNT=20
# Hall ticket range configuration for R22 regulation (entire JNTUH university)
# R22 format: 22XXXXXXXXXX (12 digits total)
# Covers all colleges: 220000000001 to 229999999999
HALL_TICKET_START=220000000001
HALL_TICKET_END=229999999999
# Delay between requests (milliseconds) to avoid rate limiting
SCRAPER_DELAY_MS=100
# Request timeout (milliseconds)
SCRAPER_TIMEOUT_MS=10000
# Max retries for failed requests
SCRAPER_MAX_RETRIES=3

# Cron Job Configuration
# Secret key for authenticating cron job requests
CRON_SECRET=your-secret-key-here
# Watcher interval (in cron format, default: every 1 minute)
WATCHER_CRON_SCHEDULE=* * * * *

# API Configuration
# API secret for protected endpoints
API_SECRET_KEY=your-api-secret-key-here
# Enable/disable Redis caching
ENABLE_REDIS_CACHE=true
# Cache TTL in seconds (default: 7 days)
CACHE_TTL_SECONDS=604800

# Environment
NODE_ENV=development

# Vercel Configuration (for deployment)
# These are automatically set by Vercel, but can be overridden
# VERCEL=1
# VERCEL_ENV=production
# VERCEL_URL=your-app.vercel.app
